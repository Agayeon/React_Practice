import { createContext } from "react";

export const TodoContext = createContext();

// 상태 값 전달용
export const TodoStateContext = createContext();

// dispatch 함수 전달 Context
export const TodoDispatchContext = createContext();
